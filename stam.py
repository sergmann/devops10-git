#!/bin/env python
n = int(input())
sled = n + 1
pred = n - 1
print = ("The next number for the number {} is {}.".format(n,sled))
print = ("The previous number for the number {} is {}.".format(n,pred))
